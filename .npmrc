# Resolve peer dependency conflicts gracefully in the monorepo.
# Needed because ioredis-mock@8 declares peer ioredis@"^5" via @types/ioredis-mock,
# which conflicts with any workspace that uses ioredis@^6.
legacy-peer-deps=true
